请继续 `D:\codex\Terrarium_Project` 项目。

工作规则：

| 项 | 要求 |
|---|---|
| 回复语言 | 中文 |
| 工作方式 | 少问，直接做；每轮尽量多推进 |
| 验证 | 勤验证，至少专项测试 + 必要时全量测试 |
| 文档 | 阶段文档放入 `项目文档/阶段规划`，总计划放入 `项目文档/总计划`，交接放入 `项目文档/交接` |
| Git | 完成稳定批次后 commit，必要时 push |
| AI 边界 | AI 不直接改 Python dataclass / WorldState schema；生成内容必须走 proposal / validator / audit / apply |

当前基线：

| 项 | 内容 |
|---|---|
| 项目路径 | `D:\codex\Terrarium_Project` |
| 远端 | `https://github.com/csaw8/simulator.git` |
| 当前已推送提交 | `2ff64f6 chore: harden and validate v5 template flow` |
| 当前阶段 | V5 已基本封口，准备进入 V6 |
| 最大缺口 | 缺少 CivilizationArc / EraModel / DevelopmentDomain 大历史层 |

下一轮目标：

```text
开始 V6-A：CivilizationArc 大历史层最小 schema。
```

请先读取这些文件：

| 文件 | 目的 |
|---|---|
| `项目文档/总计划/最终目标与后续阶段总计划_2026-06-13.txt` | 最终目标、V6-V10 规划 |
| `项目文档/阶段规划/V5-Z_第五阶段封口总结_2026-06-13.txt` | V5 当前完成范围 |
| `项目文档/阶段规划/V5_长跑与真实API验收_2026-06-13.txt` | 最新验收与风险 |
| `项目文档/可用命令.txt` | 当前 CLI 和 API 配置 |
| `src/world/state.py` | WorldState 聚合 |
| `src/world/civilization.py` | 当前文明对象 |
| `src/world/builder.py` | 世界初始化 |
| `src/storage/snapshots.py` | snapshot 保存读取 |
| `src/interfaces/commands.py` | CLI 命令入口 |

V6-A 推荐实现范围：

| 顺序 | 任务 |
|---:|---|
| 1 | 新增 `src/world/civilization_arc.py` |
| 2 | 定义 `CivilizationArc`、`DevelopmentDomainState`、`TerminalOutcome` 最小 dataclass |
| 3 | 定义允许的 era / domain / outcome 常量 |
| 4 | 提供 payload -> object、object -> dict、validator |
| 5 | 在 `WorldState` 增加 `civilization_arcs` 字段 |
| 6 | 在 `builder` 为每个 civilization 初始化 arc |
| 7 | 在 `snapshots` 保存/读取 arc |
| 8 | 增加最小 CLI：可以先做 `arc status` 或 `watch civ <id> arc` |
| 9 | 新增测试：schema、validator、builder 初始化、snapshot round-trip |
| 10 | 写阶段文档 `项目文档/阶段规划/V6-A_CivilizationArc最小schema_2026-06-13.txt` |

约束：

| 约束 | 原因 |
|---|---|
| 不做完整科技树 | 先做抽象层，避免过早复杂化 |
| 不做 UI | 大历史 schema 未稳定 |
| 不让 AI 自由写入 arc | 后续再做 AI proposal |
| 不改掉当前局部世界主干 | 当前 WorldState 继续作为局部可观察世界 |

完成 V6-A 后建议验证：

```powershell
python -m unittest tests.test_snapshots
python -m unittest discover -s tests
python -m compileall src tests
git diff --check
```

如果测试量太大，先跑新增专项测试和 snapshot，再做全量。
